# SmartSpend Test Case Design - Authentication

| Test Case ID | Module | Priority | Test Case Title | Precondition | Test Steps | Test Data | Expected Result | Actual Result | Automation Status | Automation Key Locators | Environment |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TC_AUTH_LOG_POS_001 | Authentication | P0 | Verify successful user authentication with valid credentials as username input | The user is already registered and exists in the database. | 1. Open the app and navigate to the Login screen.<br>2. Enter the valid username into the login field.<br>3. Enter the valid password into the password field.<br>4. Click the 'Sign In' button. | Login: lionh456<br>Password: **Aa12345 | 1. Navigate to Dashboard<br>2. See 'Hi, lionh456' on the header or appbar<br>3. Verify that the server returns HTTP 200 with a valid JWT token and matching username.<br>4. Local secure storage successfully saves the authentication token for session retention. | | Planned | loginField, passwordField, loginButton | Android |
| TC_AUTH_LOG_POS_002 | Authentication | P0 | Verify successful user authentication with valid credentials as email input | The user is already registered and exists in the database. | 1. Open the app and navigate to the Login screen.<br>2. Enter the valid email into the login field.<br>3. Enter the valid password into the password field.<br>4. Click the 'Sign In' button. | Login: junhongho@gmail.com<br>Password: **Aa12345 | 1. Navigate to Dashboard<br>2. See 'Hi, Lionh456' on the header or appbar<br>3. Verify that the server returns HTTP 200 with a valid JWT token and matching username.<br>4. Local secure storage successfully saves the authentication token for session retention. | | Planned | loginField, passwordField, loginButton | Android |
| TC_AUTH_LOG_NEG_001 | Authentication | P1 | Verify unsuccessful user authentication with wrong username input | The inputted username does not exist in the database. | 1. Open the app and navigate to the Login screen.<br>2. Enter the invalid username into the login field.<br>3. Enter any password with valid constraint into the password field.<br>4. Click the 'Sign In' button. | Login: fakeUser<br>Password: **Aa12345 | 1. Verify that server returns HTTP 404.<br>2. See a red SnackBar showing 'User not found' at the bottom. | | Planned | loginField, passwordField, loginButton | Android |
| TC_AUTH_LOG_NEG_002 | Authentication | P1 | Verify unsuccessful user authentication with wrong password input | The user is already registered and exists in the database. | 1. Open the app and navigate to the Login screen.<br>2. Enter the valid username into the login field.<br>3. Enter any invalid username into the password field.<br>4. Click the 'Sign In' button. | Login: lionh456<br>Password: Admin123* | 1. Verify that server returns HTTP 401.<br>2. See a red SnackBar showing 'Incorrect user or password' at the bottom. | | Planned | loginField, passwordField, loginButton | Android |
| TC_AUTH_LOG_NEG_003 | Authentication | P2 | Verify unsuccessful user authentication with wrong email input | The inputted email does not exist in the database. | 1. Open the app and navigate to the Login screen.<br>2. Enter the invalid email into the login field.<br>3. Enter any password with valid constraint into the password field.<br>4. Click the 'Sign In' button. | Login: fakeUser@gmail.com<br>Password: **Aa12345 | 1. Verify that server returns HTTP 404.<br>2. See a red SnackBar showing 'User not found' at the bottom. | | Planned | loginField, passwordField, loginButton | Android |
| TC_AUTH_LOG_NEG_004 | Authentication | P2 | Verify unsuccessful user authentication with no password input | The app is on the Login screen. | 1. Open the app and navigate to the Login screen.<br>2. Enter the valid username into the login field.<br>3. Click the 'Sign In' button. | Login: lionh456<br>Password: [Empty] | 1. The corresponding TextFormField transitions to the error state with a red border.<br>2. The error text "Please enter your password" appears directly below the text field.<br>3. Verify that no network request is sent to the backend login API. | | Planned | loginField, passwordField, loginButton | Android |
| TC_AUTH_REG_POS_001 | Authentication | P0 | Verify successfully user registration | The app is on the Register screen, and the test user data (username/email) does not exist in the database. | 1. Open the app and navigate to the Register screen.<br>2. Enter the valid username into the register username field.<br>3. Enter the valid email format into the register email field.<br>4. Enter the valid password into the register password field.<br>5. Enter the confirm password same as register password into confirm password field.<br>6. Click the 'Create Account' button. | Username: newUser<br>Email: newUser@gmail.com<br>Password: **Aa12345<br>Confirm Password: **Aa12345 | 1. Verify that server returns HTTP 201 (Created) with the saved user details.<br>2. See a green SnackBar showing 'Account created successfully' at the bottom.<br>3. Navigate back to the Log In screen. | | Planned | regUsernameField, regEmailField, regPasswordField, regConfirmPasswordField, registerButton | Android |
| TC_AUTH_REG_NEG_001 | Authentication | P1 | Verify unsuccessfully registration using the existed username | The app is on the Register screen, and the test user data (username/email) exists in the database. | 1. Open the app and navigate to the Register screen.<br>2. Enter the existed username into the register username field.<br>3. Enter the valid email format into the register email field.<br>4. Enter the valid password into the register password field.<br>5. Enter the confirm password same as register password into confirm password field.<br>6. Click the 'Create Account' button. | Username: lionh456<br>Email: newUser@gmail.com<br>Password: **Aa12345<br>Confirm Password: **Aa12345 | 1. Verify that server returns HTTP 400.<br>2. See a red SnackBar showing 'Username already exists' at the bottom. | | Planned | regUsernameField, regEmailField, regPasswordField, regConfirmPasswordField, registerButton | Android |
| TC_AUTH_REG_NEG_002 | Authentication | P1 | Verify unsuccessfully registration using the existed email | The app is on the Register screen, and the test user data (username/email) exists in the database. | 1. Open the app and navigate to the Register screen.<br>2. Enter the existed username into the register username field.<br>3. Enter the existed email into the register email field.<br>4. Enter the valid password into the register password field.<br>5. Enter the confirm password same as register password into confirm password field.<br>6. Click the 'Create Account' button. | Username: lionh456<br>Email: junhongho@gmail.com<br>Password: **Aa12345<br>Confirm Password: **Aa12345 | 1. Verify that server returns HTTP 400.<br>2. See a red SnackBar showing 'Email already exists' at the bottom. | | Planned | regUsernameField, regEmailField, regPasswordField, regConfirmPasswordField, registerButton | Android |
| TC_AUTH_REG_NEG_003 | Authentication | P1 | Verify Unsuccessfully registration with invalid password constraint. | The app is on the Register screen. The username does not exist, but the email already exists in the database. | 1. Open the app and navigate to the Register screen.<br>2. Enter the valid username into the register username field.<br>3. Enter the valid email format into the register email field.<br>4. Enter the invalid password format into the register password field.<br>5. Enter the confirm password same as register password into confirm password field.<br>6. Click the 'Create Account' button. | Username: newUser<br>Email: newUser@gmail.com<br>Password: fakepassword<br>Confirm Password: fakepassword | 1. The corresponding TextFormField transitions to the error state with a red border.<br>2. The error text "Needs upper, lower, digit, and @$!%*?&" appears directly below the text field.<br>3. Verify that no network request is sent to the backend login API. | | Planned | regUsernameField, regEmailField, regPasswordField, regConfirmPasswordField, registerButton | Android |
| TC_AUTH_REG_NEG_004 | Authentication | P1 | Verify Unsuccessfully registration with unmatched confirm password. | The app is on the Register screen, and the test user data (username/email) does not exist in the database. | 1. Open the app and navigate to the Register screen.<br>2. Enter the valid username into the register username field.<br>3. Enter the valid email format into the register email field.<br>4. Enter the valid password into the register password field.<br>5. Enter the anything that not match to register password field.<br>6. Click the 'Create Account' button. | Username: newUser<br>Email: newUser@gmail.com<br>Password: **Aa12345<br>Confirm Password: aaaaaaaaaaa | 1. The corresponding TextFormField transitions to the error state with a red border.<br>2. The error text "Password do not match" appears directly below the text field.<br>3. Verify that no network request is sent to the backend login API. | | Planned | regUsernameField, regEmailField, regPasswordField, regConfirmPasswordField, registerButton | Android |
